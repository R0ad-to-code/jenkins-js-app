version: '3.8'

services:
  webapp:
    build:
      context: .
      dockerfile: Dockerfile
    image: ${APP_NAME}:latest
    container_name: ${APP_NAME}
    restart: unless-stopped
    ports:
      - "3000:3000"
    networks:
      - app-network

networks:
  app-network:
    driver: bridge